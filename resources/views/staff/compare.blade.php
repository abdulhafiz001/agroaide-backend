<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Evaluation comparison</title>@vite(['resources/css/app.css', 'resources/js/app.js'])</head>
<body class="bg-stone-100 text-stone-900"><main class="mx-auto max-w-6xl space-y-6 px-4 py-8">
<a href="{{ route('staff.dashboard') }}" class="text-emerald-700 underline">Back to operations</a>
<section class="overflow-x-auto rounded-xl border bg-white p-6"><h1 class="mb-4 text-2xl font-bold">Run comparison</h1><table class="w-full text-left text-sm"><thead><tr><th>Run</th><th>Status</th><th>Samples</th><th>Model</th><th>Prompt</th><th>Policy</th><th>Accuracy</th><th>Coverage</th><th>P95 latency</th></tr></thead><tbody>@foreach($runs as $run)@php($metrics = $run->metrics ?? [])<tr class="border-t"><td class="py-3"><a class="text-emerald-700 underline" href="{{ route('staff.evaluations.runs.show', $run) }}">#{{ $run->id }}</a></td><td>{{ $run->status }}</td><td>{{ $run->sample_count }}</td><td>{{ $run->model_version_id }}</td><td>{{ $run->prompt_version_id }}</td><td>{{ $run->confidence_policy_id }}</td><td>{{ data_get($metrics, 'accuracy') === null ? 'Not measured' : number_format(data_get($metrics, 'accuracy') * 100, 1).'%' }}</td><td>{{ data_get($metrics, 'coverage') === null ? 'Not measured' : number_format(data_get($metrics, 'coverage') * 100, 1).'%' }}</td><td>{{ data_get($metrics, 'latency.p95_ms') === null ? 'Not measured' : number_format(data_get($metrics, 'latency.p95_ms')).' ms' }}</td></tr>@endforeach</tbody></table></section>
</main></body></html>
